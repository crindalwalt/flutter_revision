import "package:flutter/material.dart";

class VisaListing extends StatelessWidget {
  const VisaListing({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Visa Listing Page"),
    );
  }
}
